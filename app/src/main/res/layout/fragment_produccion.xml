<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/fondo_principal"
    tools:context=".views.ProduccionFragment"
    android:paddingTop="40dp"
    android:paddingBottom="?attr/actionBarSize">

    <!-- Botón para ordenar por fecha (toggle asc/desc) -->
    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btnOrdenFecha"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="8dp"
        android:text="Fecha ↓"
        android:textAllCaps="false"
        android:background="@drawable/btn_seleccion2"
        style="@style/letra_base_btn"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewProduccion"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/btnAgregar_P"
        android:layout_below="@id/btnOrdenFecha"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="16dp"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:clipToPadding="false"
        android:paddingBottom="16dp"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        tools:listitem="@layout/item_produccion" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btnAgregar_P"
        android:layout_width="200dp"
        android:layout_height="60dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="1dp"
        android:text="Agregar"
        android:textAllCaps="false"
        android:background="@drawable/btn_seleccion2"
        style="@style/letra_base_btn"/>

</RelativeLayout>